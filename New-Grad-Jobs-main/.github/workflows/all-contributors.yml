name: All Contributors — Auto-Credit

on:
  # Respond to "@all-contributors please add @user for type" comments
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

permissions: read-all
jobs:
  add-contributor:
    name: Add Contributor
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    # Only the maintainer or the auto-thank workflow (github-actions[bot]) can trigger
    if: |
      contains(github.event.comment.body, '@all-contributors')
      && (
        github.event.comment.user.login == 'ambicuity'
        || github.event.comment.user.login == 'github-actions[bot]'
      )
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Run All Contributors Bot
        uses: all-contributors/all-contributors-bot@db6de740d6b7697bfae1f79141aa16c34de958f5 # v2.3.0
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
